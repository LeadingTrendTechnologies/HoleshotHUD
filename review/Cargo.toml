[package]
name = "mxbo-review"
version = "0.15.0"
edition = "2021"

[features]
default = ["sqlite"]
sqlite = ["dep:rusqlite"]

[dependencies]
mxbo-hud = { path = "../overlay/hud" }
rusqlite = { version = "0.32", features = ["bundled"], optional = true }
